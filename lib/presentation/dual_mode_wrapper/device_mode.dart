/// Modo de dispositivo para el Dual Mode Wrapper.
///
/// Determina si la aplicación se visualiza en modo
/// [phone] (controlador móvil) o [desktop] (pantalla completa).
enum DeviceMode {
  phone,
  desktop,
}
